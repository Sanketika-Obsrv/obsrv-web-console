import { Typography } from '@mui/material';
import React from 'react';
const PreviewPage = () => {
  return (
    <div>
      <Typography variant='h1'>You are inside the Preview & save page</Typography>
    </div>
  );
};

export default PreviewPage;
