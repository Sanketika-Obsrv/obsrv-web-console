import { Typography } from '@mui/material';
import React from 'react';
const QueryPage = () => {
  return (
    <div>
      <Typography variant='h1'>You are inside the Query Page</Typography>
    </div>
  );
};

export default QueryPage;
